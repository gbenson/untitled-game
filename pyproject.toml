[project]
name = "untitled-game"
version = "0.0.0"
dependencies = [
    "pygame",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
